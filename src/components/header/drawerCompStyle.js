import { makeStyles } from "@material-ui/core";

export default makeStyles((theme)=>({
    drawer:{
        paddingRight: "none",
    },
    logo:{
        height: "25px",
    },
    logoContainer:{
        display: "flex",
        justifyContent: "space-between",
    },
}))